import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: [
    'privilege-filled-pharmaceuticals-night.trycloudflare.com',
    'localhost',
    '100.72.101.29',
    '135.181.86.129',
  ],
  async rewrites() {
    return [
      {
        source: "/api/:path*",
        destination: "http://localhost:3010/:path*",
      },
    ];
  },
};

export default nextConfig;
